{
  "name": "switchyomega",
  "version": "2.5.21",
  "private": true,
  "description": "SwitchyOmega browser extension monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "dev:ui": "pnpm --filter omega-target-chromium-extension dev:ui && pnpm --filter omega-web dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@11.1.0"
}
